<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- android 11 文件管理权限-->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <!-- 联网权限，Webview加载需要 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 监听通知 -->
    <uses-permission
        android:name="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
        tools:ignore="ProtectedPermissions" />
    <permission
        android:name="android.permission.GET_INTENT_SENDER_INTENT"
        android:protectionLevel="signature"
        tools:ignore="ReservedSystemPermission" />

    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- 获取应用信息 -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.GET_PACKAGE_SIZE" />

    <!-- 获取通知 -->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <uses-permission
        android:name="android.permission.GET_INTENT_SENDER_INTENT"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/ActivityMain">
        <activity
            android:name=".app.MainActivity"
            android:alwaysRetainTaskState="true"
            android:configChanges="locale|keyboardHidden|navigation|orientation|uiMode|screenSize|fontScale|screenLayout|smallestScreenSize|density"
            android:exported="true"
            android:hardwareAccelerated="false"
            android:label="@string/app_name"
            android:launchMode="standard"
            android:resizeableActivity="false"
            android:screenOrientation="portrait"
            android:theme="@style/ActivityMain"
            android:windowSoftInputMode="adjustPan|stateAlwaysHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".feature.whatsapp.WhatsAppCacheActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:launchMode="standard"
            android:taskAffinity="com.onus.demotest.pages.whatsapp.WhatsAppCacheActivity"
            android:theme="@style/ActivityMain" />

        <provider
            android:name="com.onus.demotest.data.ShareFileProvider"
            android:authorities="${applicationId}.sharefileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/sharefilepaths" />
        </provider>

        <activity
            android:name=".feature.webview.WebViewActivity"
            android:alwaysRetainTaskState="true"
            android:configChanges="locale|keyboardHidden|navigation|orientation|uiMode|screenSize|fontScale|screenLayout|smallestScreenSize|density"
            android:excludeFromRecents="true"
            android:exported="false"
            android:hardwareAccelerated="false"
            android:label="@string/app_name"
            android:launchMode="standard"
            android:resizeableActivity="false"
            android:screenOrientation="portrait"
            android:taskAffinity="com.onus.demotest.pages.webview.WebViewActivity"
            android:theme="@style/Theme.DemoTest"
            android:windowSoftInputMode="adjustPan|stateAlwaysHidden" />

        <activity
            android:name=".feature.notification.BindNotificationActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:launchMode="standard"
            android:taskAffinity="com.onus.demotest.pages.notification.BindNotificationActivity"
            android:theme="@style/ActivityMain" />

        <service
            android:name=".service.PushMonitorService"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <activity
            android:name=".feature.file.UsageStatsActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:launchMode="standard"
            android:taskAffinity="com.onus.demotest.pages.file.UsageStatsActivity"
            android:theme="@style/ActivityMain">
            <intent-filter>
                <action android:name="android.os.storage.action.MANAGE_STORAGE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".feature.video.VideoActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:launchMode="standard"
            android:taskAffinity="com.onus.demotest.pages.notification.BindNotificationActivity"
            android:theme="@style/ActivityMain" />

        <meta-data
            android:name="AF_PRE_INSTALL_NAME"
            android:value="${AF_PRE_INSTALL_VALUE}" />

    </application>

</manifest>
